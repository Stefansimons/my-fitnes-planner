import { NgbSortableTableDirective } from './ngb-sortable-table.directive';

describe('NgbSortableTableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbSortableTableDirective();
    expect(directive).toBeTruthy();
  });
});
